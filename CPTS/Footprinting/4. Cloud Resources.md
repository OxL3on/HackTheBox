## **Cloud Resources**

### **Company Hosted Servers**

```bash
for i in $(cat subdomainlist);do host $i | grep "has address" | grep inlanefreight.com | cut -d" " -f1,4;done
```

#### Google Search for AWS

```bash
intext:___ inurl:amazon.com
```

#### **Google Search for Azure**

```bash
intext:___ inurl:blob.core.windows.net 
```

**Target Website - Source Code**

Third-party providers such as domain.glass can also tell us a lot about the company's infrastructure.

Another very useful provider is GrayHatWarfare. We can do many different searches, discover AWS, Azure, and GCP cloud storage, and even sort and filter by file format.
