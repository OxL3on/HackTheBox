## Credential Hunting in Linux

Usually falls into 4 categories:

- Files
- History
- Memory
- Keyrings

### Files

- Configuration files
- Finding configuration files *(script)*
- Searching inside configuration files *(script)*
- Searching for DB *(script)*
- For notes *(script)*
- Searching for scripts *(script)*

### Cron Jobs

```
cat /etc/crontabls-la /etc/cron.*
```

### History Files

- `.bash_history`
- `.bashrc`
- `.bash_profile`

### Log Files

Types of logs:

- Application
- Event
- Service
- System logs
- Important log files *(others)*
- Searching log files *(script)*

### Finding Configuration Files

```bash
for l in $(echo ".conf .config .cnf")
do
    echo -e "\nFile extension: $l"
    find / -name *$l 2>/dev/null |
    grep -v "lib\|fonts\|share\|core"
done
```

### Searching Inside Configuration Files

```bash
for i in $(find / -name *.cnf 2>/dev/null | grep -v "doc\|lib")
do
    echo -e "\nFile: $i"
    grep "user\|password\|pass" $i 2>/dev/null |
    grep -v "#"
done
```

### Searching for Databases

```bash
for l in $(echo ".sql .db .*db .db*")
do
    find / -name *$l
done
```

### Searching for Notes

```bash
find /home/* -type f -name "*.txt" -o ! -name "*.*"
```

### Searching for Scripts

```
for l in $(echo ".py .pyc .pl .go .jar .c .sh")
do
    find /
done
```

### Searching Log Files

```
grep "accepted\|session opened\|session closed\|failure\|failed\|ssh\|password changed\|new user\|delete user\|sudo\|COMMAND=\|logs"
```

## Memory and Cache

Instead of manually searching memory use **Mimipenguin** *(requires root privileges)*

Run:

```
sudo python3 mimipenguin.py
```

### LaZagne

Keyrings store user passwords because they are encrypted using a master password.

```
sudo python2.7 laZagne.py all
```

### Browser Credentials

- **Location:**

```
~/.mozilla/firefox
```

- **Firefox password storage:**

```
logins.json
```

- **Also:**

```
python3.9 firefox_decrypt.py
```

- **With LaZagne:**

```
python3 laZagne.py browsers
```
