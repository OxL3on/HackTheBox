## Introduction to Password Cracking

### Generate Hash

```
echo -n pass | md5sum echo -n pass | sha256sum
```

- **Salt** is not a secret value because the system needs it for authentication.
- Shouldn't be reused for all passwords stored in the DB.

### Brute Force Attack

- Attempting every combination.

### Dictionary Attack

- Also known as a **wordlist attack**.
