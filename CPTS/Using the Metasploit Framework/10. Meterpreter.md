## **Meterpreter**

Meterpreter is a powerful, extensible Metasploit payload used mainly for post-exploitation. You can extend Meterpreter's functionality by loading additional components/plugins when needed. Meterpreter uses DLL injection (a technique Meterpreter uses to run its functionality inside a process.) to help make the connection stable and difficult to detect with simple checks.

When the exploit is completed, the following events occur:

- The target executes the initial stager. This is usually a bind, reverse, findtag, passivex, etc.
- The stager loads the DLL prefixed with Reflective. The Reflective stub handles the loading/injection of the DLL.
- The Meterpreter core initializes, establishes an AES-encrypted link over
the socket, and sends a GET. Metasploit receives this GET and configures the client.
- Lastly, Meterpreter loads extensions. It will always load `stdapi` and load `priv` if the module gives administrative rights. All of these extensions are loaded over AES encryption.

**MSF - Meterpreter Commands**

background  → Background current session
sessions    → Switch between sessions
shell       → Open host OS shell
ps          → View processes
migrate     → Move to another process
getuid      → Show current user
load        → Load extensions
run         → Run scripts/modules
pivot       → Manage pivot listeners
exit        → Terminate session

**Typical workflow**

Scan
↓
Identify service/version
↓
Research vulnerability
↓
Find Metasploit exploit
↓
Select Meterpreter payload
↓
Get Meterpreter session
↓
Enumerate
↓
Privilege escalation
↓
Further post-exploitation

- The **Local Exploit Suggester** (local_exploit_suggester) checks the target for potentially applicable local privilege-escalation exploits.

**Post-Exploitation Capabilities**

**`hashdump`** can extract password hashes when sufficient privileges are available. **`lsa_dump_secrets`** can also retrieve LSA secrets when running with appropriate privileges. **`lsa_dump_sam`**
