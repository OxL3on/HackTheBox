## **Absent Validation**

The most basic type of file upload vulnerability occurs when the web application `does not have any form of validation filters` on the uploaded files, allowing the upload of any file type by default.

**Identifying Web Framework**

A Web Shell provides us with an easy method to interact with the back-end server by accepting shell commands and printing their output back to us within the web browser. A web shell has to be written in the same programming language that runs the web server, as it runs platform-specific functions and commands to execute system commands on the back-end server, making web shells non-cross-platform scripts.

- **Check URL extensions**
    - Try common files like:
        - `/index.php` → PHP
        - `/index.asp` → ASP
        - `/index.aspx` → ASP.NET
    - If the page loads, it may reveal the backend language.
- **Fuzz web extensions**
    - Use **Burp Intruder** with a web extensions wordlist. (Web Extensions )
    - Automatically test many extensions instead of checking manually.
- **Use Wappalyzer**
    - Browser extension that can identify:
        - Backend language/framework
        - Web server (Apache, Nginx, etc.)
        - Operating system
        - JavaScript libraries and other technologies
- **Use web scanners**
    - Tools like **Burp Suite Scanner**, **OWASP ZAP**, or other web vulnerability scanners can help detect technologies/frameworks.

**Vulnerability Identification**

**Initial Test** : We will write **`<?php echo "Hello HTB";?>`** to `test.php`, and try uploading it to the web application.
